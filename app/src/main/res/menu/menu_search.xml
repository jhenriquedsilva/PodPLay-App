<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.raywenderlich.podplay.ui.PodcastActivity">
    <!-- This attribute declares which activity this layout is associated with by default -->

    <item
        android:id="@+id/search_item"
        android:title="@string/search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="collapseActionView|ifRoom"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>
        <!-- When and how this item should appear as an action item in the app bar -->
        <!-- A fully-qualified class name for the View to use as the action view -->
        <!-- This attribute adds a action view class -->
</menu>